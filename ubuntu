Тестирование на Windows + wsl2 / Ubuntu 20.04 LTS, пользователь например userapp/userapp123, ip server 172.22.133.129
Фронт и бэк на 2 доменах, тестово для доступа из под винды два домена пропишем в
C:\Windows\System32\drivers\etc\hosts:
172.22.133.129  front.local
172.22.133.129  back.local

Идём по ssh на сервер, вводим, энтер, ждёмс, и следующую строку

sudo su 
#вводим пароль пользователя
sudo apt update
sudo apt install -y nano apache2 git mysql-server mysql-client curl unzip php7.4 php-mysql php7.4-curl phpunit

//Что то убирающее небольшную ошибку
sudo usermod -d /var/lib/mysql/ mysql
//стартуем БД
sudo service mysql start
//настройки доступа БД
sudo mysql_secure_installation
Отвечаем-с:
n
вводимпароль для root Для базы данных/userapp123
y y y y 

Создаем в mysql бд,пользователя
mysql
CREATE DATABASE yiiaap;
CREATE USER 'user'@'localhost' IDENTIFIED BY 'qweqweqwe'; 
GRANT ALL PRIVILEGES ON yiiaap.* TO 'user'@'localhost';
FLUSH PRIVILEGES;
exit

cd /var/www/html
sudo git clone https://github.com/hrompik/yiiapp.git
cd /var/www/html/yiiapp


sudo curl -sS https://getcomposer.org/installer -o composer-setup.php
sudo php composer-setup.php --install-dir=/usr/local/bin --filename=composer

sudo chown -R userapp /var/www/


sudo nano /var/www/html/yiiapp/environments/dev/common/config/main-local.php меняем пользователя жмём контрол S контрол X
cd /var/www/html/yiiapp
php init
отвечаемс 0 y
exit

cd /var/www/html/yiiapp
composer u

sudo su
sudo a2enmod rewrite
sudo cp /var/www/html/yiiapp/apache_conf/front.local.conf /etc/apache2/sites-available/front.local.conf
sudo cp /var/www/html/yiiapp/apache_conf/back.local.conf /etc/apache2/sites-available/back.local.conf

sudo a2ensite back.local
sudo a2ensite front.local


sudo service apache2 restart

cd /var/www/html/yiiapp
sudo php yii migrate --migrationPath=@yii/rbac/migrations/
sudo php yii migrate